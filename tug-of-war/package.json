{
  "name": "tug-of-war-quiz",
  "version": "0.1.0",
  "private": true,
  "description": "雙人拔河問答小遊戲 — 沿用 SnakeG 的 Notion 學生/題庫資料，部署到 Firebase 專案 snakegame-91f56 底下的獨立 Hosting site",
  "type": "module",
  "scripts": {
    "predev": "node scripts/prepare-data.mjs",
    "dev": "vite",
    "prebuild": "node scripts/prepare-data.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^12.0.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
